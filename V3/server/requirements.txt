# Voice Avatar Server — unified requirements
# Base image (GPU): pytorch/pytorch:2.5.1-cuda12.4-cudnn9-runtime
#   torch 2.5.1 + torchaudio pre-installed via conda → SKIP_TORCH_INSTALL=true
# Base image (CPU): python:3.11-slim
#   torch + torchaudio installed explicitly via pip

fastapi==0.115.12
uvicorn[standard]==0.34.3
pydantic==2.11.4
numpy==1.26.4
soundfile==0.12.1

# ── STT: Faster-Whisper (CTranslate2 backend) ────────────────────────────────
faster-whisper>=1.0.0

# ── Translation: SMaLL-100 (100+ languages) ───────────────────────────────────
sentencepiece==0.2.0
huggingface_hub>=0.23.0

# ── TTS: OmniVoice ────────────────────────────────────────────────────────────
# omnivoice 0.1.5 hard-requires transformers>=5.3.0
omnivoice==0.1.5

# ── Shared: transformers (SMaLL-100 + OmniVoice) ─────────────────────────────
transformers>=5.3.0
# torchaudio used internally by OmniVoice
torchaudio>=2.1.0

# ── Romanisation for lipsync ──────────────────────────────────────────────────
uroman>=1.0.0
anyascii>=0.3.2

# ── Audio resampling ──────────────────────────────────────────────────────────
scipy>=1.13.0
