# Voice Avatar Server — unified requirements
# Base image (GPU): pytorch/pytorch:2.5.1-cuda12.4-cudnn9-runtime
#   torch 2.5.1 + torchaudio pre-installed via conda → SKIP_TORCH_INSTALL=true
# Base image (CPU): python:3.11-slim
#   torch installed explicitly via pip

fastapi==0.115.12
uvicorn[standard]==0.34.3
pydantic==2.11.4
numpy==1.26.4
soundfile==0.12.1

# ── Model 1: Faster-Whisper large-v3 (STT + language detection) ──────────────
# CTranslate2 backend — ~4× faster than vanilla HuggingFace transformers.
# Audio I/O is handled internally; no torchaudio needed.
faster-whisper>=1.0.0

# ── Model 2 (utility): SMaLL-100 (translation, 100+ languages) ───────────────
sentencepiece==0.2.0
huggingface_hub>=0.23.0

# ── Model 3: OmniVoice TTS ────────────────────────────────────────────────────
# omnivoice 0.1.5 hard-requires transformers>=5.3.0
omnivoice==0.1.5

# ── Model 4: Granite 4.0 Nano LLM ────────────────────────────────────────────
# transformers + accelerate cover both Whisper and Granite
transformers>=5.3.0
accelerate>=0.30.0

# ── Romanisation for translated text ─────────────────────────────────────────
uroman>=1.0.0
anyascii>=0.3.2

# ── Audio resampling ──────────────────────────────────────────────────────────
scipy>=1.13.0
