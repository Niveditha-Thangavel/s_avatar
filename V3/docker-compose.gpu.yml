services:
  vllm-omni:
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: all
              capabilities: [gpu]
  # Orchestrator has no GPU dependency (CTranslate2 runs CPU-only for IndicTrans2)
